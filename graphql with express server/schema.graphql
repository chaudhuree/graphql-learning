type Query {
  job(id: ID!): Job
  jobs: [Job!]
  company(id: ID!): Company
}

type Job {
  id: ID!
  title: String!
  """
  _date_ is generated customly
  """
  date: String!
  description: String
  company: Company!
}

type Company {
  id: ID!
  name: String!
  description: String
}
